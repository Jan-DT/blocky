package nl.jandt.block.example;

import nl.jandt.blocky.engine.core.Container;
import nl.jandt.blocky.engine.core.Trait;

public class TestTrait extends Trait {
    public TestTrait(Container container) {
        super(container);
    }
}
